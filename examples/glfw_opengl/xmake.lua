target("ex-glfw")
    set_kind("binary")
    add_files("*.cc")
    add_deps("ecs", "xc-glfw-support","xc-opengl-support")
    add_deps("xcal2")
    add_deps("xcal-opengl-render")
    add_deps("xcal-camera-ui-controler")
    add_defines("USE_GLBINDING")
